# replication:
#   connections:
#     - type: postgresql
#       uri: postgresql://postgres:postgres@postgres:5432/postgres

#       # SSL settings
#       sslmode: disable # 'verify-full' (default) or 'verify-ca' or 'disable'

# # Connection settings for sync bucket storage
# storage:
#   type: mongodb
#   uri: mongodb://mongo:27017/powersync_demo

# # The port which the PowerSync API server will listen on
# port: 8080

# # Specify sync rules
# sync_rules:
#   bucket_definitions:
#       user_worktrackers:
#         parameters:
#           - name: user_id
#             type: integer
#         data:
#           - SELECT * FROM WorkTrackers WHERE user_id = :user_id
#           - SELECT * FROM Addresses
#           - SELECT * FROM Bleachers

# # Settings for client authentication
# client_auth:
#   # Enable this if using Supabase Auth
#   supabase: true

#   # JWKS URIs can be specified here.
#   jwks_uri: [TODO]

#   # JWKS audience
#   audience: ['powersync-dev', 'powersync']

#   # Settings for telemetry reporting
#   # See https://docs.powersync.com/self-hosting/telemetry
#   telemetry:
#     # Opt out of reporting anonymized usage metrics to PowerSync telemetry service
#     disable_telemetry_sharing: false
